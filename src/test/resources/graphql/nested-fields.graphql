# TODO: verify actual nested relation field name via introspection
query ProductsWithNestedFields($first: Int!) {
  products(first: $first) {
    id
    name
    category {
      id
      name
    }
  }
}
